# Pixel2ASCII
Pixel2ASCII is a Python-based project that converts 24-bit BMP images into ASCII art using direct byte-level image processing. The project avoids all image-processing libraries and manually parses BMP headers, pixel data, and grayscale values to generate ASCII output in the terminal and text files.
